# 2026-03-31

## 待继续推进：手写脚本直转九宫格分镜图

- 用户提出新增一种产品脚本生成方式：允许用户输入自己已经写好的脚本内容，由系统自动将该脚本转化为九宫格分镜图提示词，或后续扩展为逐镜头分镜图提示词。
- 当前已完成产品方向收口：先不同步改两张表，**第一阶段先做 `003产品脚本分镜图生成`**，暂不优先推进 `003-2分镜头脚本生成`。
- 已确认采用的入口方案是：在现有表内新增“脚本来源”模式，而不是额外新建独立链路。
- 第一阶段确定的方案为：
  - 在 `003产品脚本分镜图生成` 表新增至少两个字段：`脚本来源`、`手写脚本内容`。
  - `脚本来源` 至少支持：`自动生成`、`手写脚本`。
  - 当 `脚本来源=自动生成` 时，维持现有 `tk_script_gen.py -> tk_storyboard.py` 流程不变。
  - 当 `脚本来源=手写脚本` 时，不再走“自动生成产品脚本”的逻辑，而是在 `tk_script_gen.py` 内新增分支：读取 `手写脚本内容`，先做一次轻量标准化/整理，再写入现有产品脚本结果字段，然后继续复用现有 `tk_storyboard.py` 产出九宫格分镜图。
- 关键设计原则已经确定：
  - **只动入口，不动后链路接口**。
  - **后续 `tk_storyboard.py` 尽量无感知脚本来源**，只消费统一脚本字段。
  - 第一版尽量把改动集中在 `tk_toolkit_dual/tk_script_gen.py`，必要时再少量查看 `common.py` / `tk_dispatcher.py`。
- 第一阶段成功标准：
  1. 用户在 `003产品脚本分镜图生成` 中填写 `脚本来源=手写脚本` 与 `手写脚本内容`。
  2. `tk_script_gen.py` 能识别手写模式并成功写回标准化后的脚本。
  3. `生成状态` 正常进入成功。
  4. `tk_storyboard.py` 能继续基于该脚本正常产出九宫格分镜图。
- 后续演进方向已讨论但暂不马上做：第二阶段再考虑把相同能力扩展到 `003-2分镜头脚本生成`，实现“手写逐镜头脚本直转逐镜头提示词/结构化 JSON”。
- 用户明确要求：今晚先把以上结论写入 memory，明天继续推进，不要遗忘。

## 已落最小可用改动：003 手写脚本入口

- 已实际修改 `tk_toolkit_dual/tk_script_gen.py`，按最小可用方案新增 `脚本来源=手写脚本` 分支。
- 当前实现逻辑：
  - 若 `脚本来源` 为 `手写脚本` / `手动填写` / `手写`（兼容若干别名），则读取 `手写脚本内容`。
  - 使用 Gemini 做一次**轻量整理/标准化**，目标是保留用户原意，只补足到适合九宫格分镜生成的脚本文本。
  - 整理后的结果仍然写回既有字段 `生成的脚本`。
  - 成功后仍沿用原逻辑把 `分镜图状态` 置为 `待执行`，让现有 `tk_storyboard.py` 继续消费，不改后链路。
- 自动生成模式仍保留原逻辑：参考爆款分析 → 策略提炼 → 生成脚本。
- 已完成 `python3 -m py_compile tk_toolkit_dual/tk_script_gen.py` 语法自检，通过。
- 相关代码提交：`37da91c Add handwritten script path for 003 storyboard flow`。

## 联调结果

- `ryan` 实例已完成真实验证：用户在 003 表填写 `脚本来源=手写脚本` 与 `手写脚本内容` 后，表中已观察到任务成功产出，说明链路 `手写脚本 -> 生成的脚本 -> 九宫格分镜图` 已经跑通。
- `colleague` 实例也已完成触发验证：`2026-03-31 09:56:36` dispatcher 日志已出现 `产品脚本生成` 新任务启动，record_id=`recvfpepnyx3ae`，证明新增字段与现有调度链路兼容，手写脚本任务已被正常拉起。
- 因此截至 2026-03-31 上午，这个最小可用功能已经在双实例环境完成落地，并至少在 `ryan` 真实业务表中跑通。

## 003 表内视频生成首轮线上排障

- `ryan` 实例的 `003视频生成` watch 在首轮测试时一开始未被消费，根因是常驻 dispatcher 还在跑旧版本进程；重启 `com.ryan.tk-dispatcher.ryan` 后，新 watch 生效并成功拉起记录 `recvf8PySi1U91`（`task0051`）。
- 该条记录随后长时间停留在 `视频生成状态=生成中`。排查确认并非 dispatcher 未触发，而是子进程 `tk_video_from_storyboard.py` 已持续运行十几分钟未退出，且表里只有 `视频提示词`，没有 `视频任务ID` / `生成视频`。
- 由此确认卡点发生在 **提交 `sora` 任务拿到 `视频任务ID` 之前**，而不是后续轮询完成后回传失败。
- 已对 `tk_toolkit_dual/tk_video_from_storyboard.py` 做最小修复，目标是避免“表面一直生成中但真实卡死不可见”：
  - 为 `submit_sora_task(...)` 增加显式提交超时与 `requests` 异常分类，避免长时间无感卡住。
  - 增加提交前后日志：下载分镜图开始/成功、提交 `sora` 开始/成功。
  - 若提交成功，第一时间写回 `视频任务ID`，不再等到更后面阶段。
  - 在 `poll_sora_task(...)` 中增加轮询进度回调，按状态变化或每 60 秒把 `status / elapsed / task_id` 写到 `视频错误信息`，让表内可见真实进度。
  - 成功完成后清空 `视频错误信息`，避免保留中间态文案。
- 已完成 `python3 -m py_compile tk_toolkit_dual/tk_video_from_storyboard.py` 语法自检，通过。

## 2026-03-31 晚间继续排查结论（003 视频链路）

- 实际读取了：
  - `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/config.ryan.json`
  - `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/tk_dispatcher.py`
  - `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/tk_video_from_storyboard.py`
  - `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/run_dispatcher_instance.sh`
  - `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/launchd.ryan.out.log`
  - `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/launchd.ryan.err.log`
  - 飞书 003 表：`LBWUbgRfEavAgjsXNIhcpo0Dnvb / tbl2Yp6T4jDN8Rfr`
- 已确认当前 `run_dispatcher_instance.sh` 是前台 `exec` 版本，不再是旧的 `nohup ... &` 后台化实现。
- `launchctl print gui/$(id -u)/com.ryan.tk-dispatcher.ryan` 显示：
  - `state = running`
  - `pid = 4119`
  - `program = /Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/run_dispatcher_instance.sh`
  - `TK_CONFIG_FILE => /Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/config.ryan.json`
  - `TK_INSTANCE => ryan`
  - `runs = 1189`
  - `immediate reason = inefficient`
  - `last terminating signal = Terminated: 15`
- `launchd.ryan.out.log` 里仍出现大量重复：
  - `dispatcher started: instance=ryan pid=...`
  - `dispatcher exec: instance=ryan ...`
  说明 launchd 层面存在反复启动/重复拉起现象，未收口为稳定单次常驻观察态。
- 003 表中已明确确认：
  - `recvf8PySi1U91 / task0051`
    - `视频模型 = sora`
    - `视频生成状态 = 失败`
    - `视频任务ID = 空`
    - `视频错误信息 = 错误[UPSTREAM_RATE_LIMIT]: Sora任务提交请求失败: 429 Client Error: Too Many Requests for url: https://own-jarvis-api.com/v1/video/create`
  - 这说明当前失败根因已从“卡死/无反馈”收敛为**上游 `https://own-jarvis-api.com/v1/video/create` 在提交阶段返回 429**。
  - 因为提交失败，所以没有 `视频任务ID`，这个结果是符合当前代码路径的。
- 继续查看同表新记录时，又看到：
  - `recvf8PzD3tIbv / task0052`
    - 现状态也已变成 `视频生成状态 = 失败`
    - `视频错误信息 = 错误[UPSTREAM_RATE_LIMIT]: Sora任务提交请求失败: 429 Client Error: Too Many Requests for url: https://own-jarvis-api.com/v1/video/create`
  - 说明不是单条坏单，而是当前 `sora` 提交路径在这个时间窗口里连续被上游限流。
- 本轮还暴露了一个排查细节：执行环境里 `rg` 不存在（`zsh:1: command not found: rg`），后续查进程/grep 需要避免依赖 `rg`。
- 下一步应优先继续查：
  1. `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/dispatcher.ryan.log`
  2. `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/dispatcher-runtime.ryan.log`
  3. `.dispatcher_heartbeat.ryan.json`
  4. 当前存活的 `tk_dispatcher.py` / `tk_video_from_storyboard.py` 进程
  以区分“上游限流导致业务失败”和“launchd/dispatcher 本身仍不稳定”这两类问题。

## 2026-03-31 22:34 继续修：003 视频限流处理链路

- 已实际修改 `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/common.py`：
  - 调整 `build_error_payload(...)` 判定顺序，把 `429` / `rate limit` / `too many requests` 提前到 `timeout` / `connection` 之前。
  - 目的：避免错误文本同时包含 `429` 与网络/超时关键词时，被误归类成 `UPSTREAM_NETWORK`，保证 `UPSTREAM_RATE_LIMIT` 优先命中。
- 已实际修改 `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/tk_dispatcher.py`：
  - 将 `003视频生成` watch 的 `max_retries` 从 `1` 改为 `0`，避免 429 后短时间内再次立即重打上游。
  - 为 `003视频生成` watch 新增：
    - `circuit_threshold = 1`
    - `circuit_cooldown_seconds = 1800`
  - 并让 `record_circuit_failure(watch)` 支持优先读取 watch 级别熔断参数，而不是只读全局 `dispatcher.circuit_breaker`。
  - 目的：让 `tk_video_from_storyboard.py` 一旦命中 `UPSTREAM_RATE_LIMIT`，能更快熔断并拉长冷却窗口，减少连续撞限流。
- 已执行语法自检，通过：
  - `python3 -m py_compile /Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/common.py /Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/tk_dispatcher.py /Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/tk_video_from_storyboard.py`
- 已提交代码：`a10ec8d Harden 003 video rate-limit handling`
- 这一轮仍然是“最小可用收口”，还没有做更大规模的状态机扩展；核心目标是先阻止 `003视频生成` 在 `sora` 429 时反复撞上游，并保证错误码归类稳定。
