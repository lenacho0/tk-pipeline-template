# 2026-05-20

## 003-3 Veo 3.1 首帧生视频修正

- 用户明确指出 003-3 需要的是“分镜图作为首帧生视频”，不是参考图生视频；同时旧输出一直是横屏，不符合竖屏需求。
- 根因确认：AIHubMix `/v1/videos` multipart `input_reference[]` 路径更接近参考图/兼容接口语义，实测即使传 `aspect_ratio=9:16`、`720x1280` 等参数，返回仍是 `1280x720` 横屏，不适合作为正式 003-3 首帧模式。
- 已将 `tk_toolkit_dual/tk_shot_video.py` 正式路径切换到 AIHubMix Gemini proxy + Google GenAI SDK 原生 `client.models.generate_videos(...)`：用 `image=types.Image(...)` 传 003-3 `分镜图` 作为首帧，只传首帧，不传尾帧，不传 `referenceImages`。
- Native Veo 配置使用 `types.GenerateVideosConfig(duration_seconds=..., resolution="720p", aspect_ratio="9:16")`；配置表记录 `recvk4lsU0TGGD` 的 `API 代理地址` 已更新为 `https://aihubmix.com/gemini`，模型仍为 `veo-3.1-fast-generate-preview`。
- 单元测试新增/更新：`test_call_native_veo_uses_image_as_first_frame_and_portrait_config` 验证 `image=` 首帧、`aspect_ratio=9:16`、`resolution=720p`、不传 `last_frame`；`test_run_generation_uses_native_first_frame_submitter` 验证正式 worker 使用 native submit/poll/download 路径。
- 验证命令通过：`python3 -m unittest tk_toolkit_dual/test_shot_video.py -v`；`env PYTHONPYCACHEPREFIX=/private/tmp/workspace-tk-pycache python3 -m compileall -q tk_toolkit_dual/tk_shot_video.py tk_toolkit_dual/test_shot_video.py`。
- 真实 smoke test 成功：003-3 记录 `recvk2MLFnjq1K`，task_id `models/veo-3.1-fast-generate-preview/operations/i395w6ei29b9channel4989`，本地视频 `/Users/ryanlynn/.openclaw/workspace-tk/shot_video_work/recvk2MLFnjq1K/recvk2MLFnjq1K_video.mp4`，飞书 file_token `UJAVbECCRoUE4UxUtEtc1YC7nNc`。
- `ffprobe` 验证新输出为 `720x1280`、`4.000000s`，已确认不再是横屏 `1280x720`。
- 13:08 后用户批量生成 003-3 Veo 视频时，记录失败并显示 `dispatcher兜底失败回写[RUNTIME_BUG]: 404 Not Found`。根因不是 Veo 生成失败：失败记录已写入 `视频任务ID`，手动轮询 operation 可见 `done=True` 且返回 `response.videos[].bytesBase64Encoded` 内联 mp4；本地脚本原先只兼容 SDK `generated_videos[].video` / file 下载路径，导致后续下载阶段 404。二次重试仍失败的直接原因：Google SDK 的 `operations.get()` 会把原始 `models/.../operations/...channel...` 改写成 `projects/.../publishers/google/models/.../operations/...`，AIHubMix raw operation 接口不接受改写后的名字。已修复 `tk_shot_video.py`：保存并复用原始 operation name 做 raw fallback；轮询时 SDK 若丢失 response 则取 raw operation dict；下载时兼容内联 base64 mp4；重跑已有 `视频任务ID` 的失败记录时优先复用旧 operation，避免重复提交烧模型。相关测试已通过 `.venv312` 的 `python -m unittest tk_toolkit_dual/test_shot_video.py -v`；已手动恢复并写回 4 条失败记录：`recvk7Xf5MASU0`、`recvk7Xf5MU6Gx`、`recvk7Xf5MkmQJ`、`recvk7Xf5MdO1X`，均为 `720x1280`。

## 脚本文档逐分镜生成表

- 早先新增过 ryan Base 单表 `脚本文档-逐分镜生成表`，table_id `tbl7ApL0SWeC22dR`；该单表方案后续已被下面的三表拆分方案取代，正式链路不要再指向旧单表。
- 新链路支持一条文档母记录粘贴整篇脚本文档，由 `tk_script_doc_shots.py parse` 解析出全局参考资产、逐分镜记录和每条分镜的参考图需求。
- 参考图规则已按用户要求定为“按每条分镜结构化需求选择”，不是每条都上传人物/环境/产品：分镜字段 `需要产品参考图` 与 `参考资产ID列表` 决定本条生成时上传哪些参考图。
- 宠物/环境/人类角色底图作为 `参考底图记录` 生成并人工审核；分镜图生成只使用已审核通过且被该分镜点名的底图。产品图从现有产品表读取，优先用新表 `关联产品记录` 链接字段，兼容 `关联产品` 文本按名称查找。
- dispatcher 新增 5 个 watch：脚本文档解析拆分、参考底图生成、口播音频生成、分镜图生成、分镜视频生成。

## 脚本文档逐分镜链路拆表

- 按用户要求将原 `脚本文档-逐分镜生成表` 78 字段单表方案改为 3 张表：`脚本文档-任务表` `tblBC37ktQBHPLep`、`脚本文档-参考资产表` `tblFg33rvB7eCyzr`、`脚本文档-分镜生产表` `tbldPJLJhczlGzSt`。
- `tk_toolkit_dual/config.json` / `config.ryan.json` 已写入 `script_doc_tasks`、`script_doc_reference_assets`、`script_doc_shots`；`script_doc_shots` 现在指向分镜生产表，不再指旧单表。
- `tk_script_doc_shots.py parse` 现在从任务表读取母任务，并把参考资产/分镜分别写入独立表；参考图收集从参考资产表读取审核通过底图，父任务产品信息从任务表读取。
- ryan dispatcher 已重启，5 个脚本文档 watch 已按表拆分：解析监听任务表，参考底图监听参考资产表，口播音频/分镜图/分镜视频监听分镜生产表。
- 三张表已创建日常视图：任务表 `01-用户入口`/`99-解析排错`；参考资产表 `01-参考图确认`/`99-参考图排错`；分镜生产表 `01-分镜图生成`/`02-口播音频`/`03-分镜视频`/`04-发布素材`/`99-排错`。
- 验证通过：46 个相关 unittest 通过；compileall 覆盖脚本文档解析/建表/dispatcher/分镜图/音频/视频/common。
- 三表真实 smoke test 已跑通：任务记录 `recvk8uUo5tb4o`，批次 `SCRIPTDOC-20260520134303-o5tb4o`，生成参考资产 `pet_1`/`env_1` 并审核通过；分镜 1 `recvk8vXL2vhL4` 为 `需要产品参考图=否`、分镜图 reference_count=2、口播成功；分镜 2 `recvk8vXL2JSsU` 为 `需要产品参考图=是`、分镜图 reference_count=3、口播成功、Veo 视频成功。
- smoke 视频产物：task_id `models/veo-3.1-fast-generate-preview/operations/zzctwataj7ubchannel5096`，本地 `/Users/ryanlynn/.openclaw/workspace-tk/shot_video_work/recvk8vXL2JSsU/recvk8vXL2JSsU_video.mp4`，飞书 file_token `C3UGbM8Olox6UTxBoSlctsnon04`，`ffprobe` 为 `720x1280`、`4.000000s`。
- smoke test 暴露并修复两个写回/路径 bug：`safe_download_attachment()` 返回布尔 `True` 时不能被当作路径字符串；`分镜视频URL` 直接 OpenAPI 写回需用 `{"link": url, "text": url}`，不能写裸字符串。相关回归测试已加入，当前相关 unittest 为 47 个通过。
