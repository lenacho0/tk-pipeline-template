# 2026-03-29

## 调度器巡检误报排查

- 用户反馈“调度器为啥没运行”。
- 实查确认 dispatcher 实际仍在运行：
  - launchctl 中存在 `com.ryan.tk-dispatcher`
  - `tk_dispatcher.py` 进程仍存活
  - `.dispatcher_heartbeat.json` 持续更新，状态为 `running`
- 误报根因定位到 `tk_toolkit/tk_healthcheck.py`：
  - 原逻辑过度依赖 `pgrep -f tk_dispatcher.py`
  - 当次巡检读到了新鲜 heartbeat，但因 `pgrep` 未命中，仍误判为“调度器未运行”
- 已修复：
  - `check_dispatcher()` 改为优先接受“最近 180 秒内的 running heartbeat”作为运行依据
  - 日志摘要优先读取 `dispatcher-runtime.log`，不再只看旧的 `dispatcher.log`
- 本地复测通过：健康巡检现已正确显示 dispatcher 为“运行中”。

## 今日无任务拉起的进一步结论

- dispatcher 主循环在跑，但今日 metrics 为 `launched={}`，说明今天没有成功拉起新任务。
- 曾在 `08:22:26` 记录到一次 `[视频制作] 读取表失败: 400 Client Error`。
- 进一步实测确认：
  - 当前 `TABLE_VIDEO=tblQdTGSsmQbPMQd` 配置正确
  - 当前可正常读取视频制作表记录
  - 当前也可正常列出该 Bitable 下所有 tables
- 因此，08:22 那次更像是瞬时 Feishu/Bitable 请求异常，不像持续性配置错误。
