# 2026-03-30

## TK pipeline / 双表流程切换记录

- colleague 新双表流程在真实环境完成了一轮从抓取 → 分析 → 产品脚本生成 → 分镜图生成 → 逐镜头脚本生成 → 逐镜头图片生成的贯通验证。
- 分析环节早期失败的根因不是表结构或字段缺失，而是 `tk_analyze.py` 在 colleague 线上 4 并发 + 900 秒超时下被 dispatcher 批量超时杀掉；单跑验证表明任务本身可成功，主要耗时在 Gemini 分析结果格式不稳定后触发二次 `reformat`。
- 已对 colleague 线分析配置做最小修复：`tk_analyze.py` 从 `max_concurrency: 4` / `timeout: 900` 调整为 `max_concurrency: 1` / `timeout: 2400`，随后失败分析记录补跑成功。
- 逐镜头脚本生成最初“完全不触发”，最终定位并非表 ID 错误、也不是字段不匹配，而是 **dual colleague dispatcher 没真正作为正式实例接管**，导致一直被旧/错实例状态污染。
- 关键排查结论：
  - `003-2分镜头脚本生成` 表 ID `tbljvIqVfcufrWBZ` 正确，字段 `生成状态`、`逐镜头脚本` 等与脚本预期匹配。
  - 表内存在真实 `生成状态=待生成` 记录，证明不是“没数据”。
  - old single-table dispatcher (`/tk_toolkit/tk_dispatcher.py`) + dual default + dual colleague 曾同时存在，造成实例混跑风险。
- 已完成运行时收口：
  - 旧单表 launchd job `com.ryan.tk-dispatcher` 已 bootout / disable / 删除 plist / 停止进程。
  - 当前正式保留的常驻实例为：`com.ryan.tk-dispatcher.ryan` 与 `com.ryan.tk-dispatcher.colleague`。
  - 老流程已停用，双表流程已接管运行时。
- 切换策略结论：
  - 双表流程已具备灰度上线条件，并在实际运行层面完成切换。
  - 不建议立刻删除旧代码资产；当前做法是“停用旧运行链路，保留回退代码/配置”。
  - 截至 2026-03-30 夜间，本轮真实验证链路已跑通，当前判断双表流程运行层面没有明显阻塞问题，可在次日继续按正常业务方式做实测。
  - 后续重点继续观察：dispatcher 幂等性、重复触发、状态回滚、以及 `003-2/003-3` 多样本稳定性。
- 2026-03-30 晚间进一步确认：`待生成` 长时间不消费的主故障根因是 `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/run_dispatcher_instance.sh` 旧实现使用 `nohup ... &` 后台化，导致 launchd 托管的是壳脚本而不是 dispatcher 主进程，`KeepAlive` 触发错误重启；修复为前台 `exec` 后，`com.ryan.tk-dispatcher.colleague` / `com.ryan.tk-dispatcher.ryan` 恢复常驻，任务重新可从 `待生成` 进入 `生成中`。
- 同日晚间继续确认第二层问题：record `recvfluIiofv5H` 一度已经 `生成状态=成功`、但 `分镜图状态=生成中`。后续通过 colleague dispatcher 补跑后，该记录于 `2026-03-30 21:11:40` 被重新拉起 `分镜图生成`，并于 `2026-03-30 21:13:27` 成功完成，输出文件为 `/Users/ryanlynn/.openclaw/workspace-tk/tk_toolkit_dual/workspace_colleague/storyboard_work/recvfluIiofv5H/recvfluIiofv5H_storyboard.png`，对应统计增至 `launched=8`、`success=5`。当前该条坏单已解除。
- 从当晚后续 `dispatcher-runtime.colleague.log` 观察，最近时段未见新的 `分镜图生成` 启动后无收尾记录；因此当前问题重心已从单条故障转为次日继续做新增样本验证，确认是否还会新增 `分镜图状态=生成中` 残留。

## 用户偏好 / 工作方式

- 用户明确要求重要操作和结论写入 memory，保留持续上下文。
- 用户偏好直接、可执行的排障与切换建议，不喜欢空转汇报。
