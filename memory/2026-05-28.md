# 2026-05-28

## 故事板图片视频生成表：提示词真源与重生成链路

- 围绕 `001-故事板图片视频生成表` 落地了故事板图片 + Omni 图生视频链路的多轮修正：母任务由用户填 `脚本内容`、选择产品记录和一个或多个模特记录，可选环境图；产品图/产品名/目标人群来自 `初始化-产品信息`，模特照片和角色描述来自 `初始化-模特形象配置`。
- `001-故事板图片视频生成表` 的故事板图片生成侧新增用户可选字段：`故事板图片模型`、`故事板图片画面尺寸`、`故事板图片画面比例`。默认故事板图片为 `gpt-image-2` / `1280x720` / `16:9`；代码生成 Storyboard 分段时写入默认值，旧分段空字段会回填，图片生成时优先读取记录字段而不是依赖全局配置里的空尺寸。
- 修复 `001-故事板图片视频生成表` 视图混乱：`01-母任务入口` 只筛选 `记录类型=母任务`，`02-故事板图片` / `03-Omni视频` 只筛选 `记录类型=Storyboard分段`；dispatcher 三个故事板 watch 也按记录类型强约束，避免母任务/分段/空记录被错误阶段捡走。同步时修正了 1 条误标成母任务的旧分段，并给 1 条空任务名母任务补默认名称。
- 排查 `recvkTjxA7DOgF_storyboard.png` 人物/产品跑偏：父任务、关联产品、选择模特、环境图和本地下载参考图都正确；根因是拆分后的 `故事板图片提示词` 继续使用脚本文字里的服装/产品外观（蓝衬衫黑裙、白T短裤、透明喷雾绿白标签），与所选模特/产品参考图冲突，且 OTU 图片接口只有第一张图作为强 `image_base64`，其他参考图主要在 `metadata.urls` 中。已修复：拆分提示词新增“参考图优先于脚本文字外观”，图片生成前合成 `reference_contact_sheet.png`（产品 + 所有模特 + 环境）作为主垫图，并在运行时 prompt 中强制参考图覆盖冲突文字。
- 故事板拆分提示词改为读取 `初始化-模型与API配置` 的 `故事板图片提示词拆分`。模型输出 Markdown 代码块中的 `Storyboard XX Prompt:`，代码只解析段落并原样写入子记录 `故事板图片提示词`，不再用代码模板二次改写最终图片 prompt。
- Omni 视频提示词改为读取配置表 `故事板视频生成-Omni` 的 `提示词`，默认内容为用户给定的 Markdown prompt。最终提交给视频 API 的 prompt 只剥离 Markdown 代码块和 `Omni Video Prompt:` 标题，不追加产品、Time Range、故事板提示词、参考图顺序或额外视频方向。
- 用户明确要求：最终提示词不要额外做其他功能，必须直接使用用户给出的系统提示词；任何增删改都要事先确认。
- 回归验证：`python3 -m unittest tk_toolkit_dual.test_storyboard_video` 通过 29 tests；`python3 -m unittest tk_toolkit_dual.test_storyboard_video tk_toolkit_dual.test_shot_video tk_toolkit_dual.test_script_doc_shots tk_toolkit_dual.test_shot_storyboard` 通过 86 tests。

## 首尾帧 Markdown 直拆与配置拆分

- `002-首尾帧视频生成表` 已改为严格 Markdown 直拆：只接受 `## S01 场景标题` + `### S01-1 首帧生图提示词` / `### S01-2 尾帧生图 / 编辑提示词` / `### S01-3 首尾帧图生视频提示词`。系统只提取代码块或标题下正文原文，不调用 Gemini，不做 JSON 兜底，不做自然语言改写；格式不对直接失败并提示修正。
- 飞书 `初始化-模型与API配置` 已拆清：旧 `文本拆分生成-Gemini` 改为 `脚本文档结构化拆分-Gemini`，只服务 003 三张脚本文档表；`故事板图片提示词拆分-Gemini` 补齐独立 Gemini 模型/API/提示词配置，只服务 001；002 只挂 `图片生成-OTU` 和 `分镜视频生成-OTU`。
- 本地配置 key 改为 `script_doc_text_split=recveQZPORmm33`、`storyboard_text_split=recvkP9VfsEY3o`、`main_image_otu=recveQZRZc7NUI`。后续不要再使用 `main_text_split` 或 `文本拆分生成-Gemini` 作为正式运行口径。
- 验证：`py_compile` 通过；相关单测 `130 tests OK`；`rg "文本拆分生成-Gemini|main_text_split|复用文本拆分生成-Gemini" tk_toolkit_dual -S` 无命中；ryan dispatcher 已 `launchctl kickstart`，healthcheck 显示 `Markdown直拆✓`。
- 排查 `002-首尾帧视频生成表` 记录 `recvkRvBsEC3aQ`（尿味test-场景04-阳台瓷砖）长期停在首帧图“生成中”：06:36:18 已提交 OTU 任务 `task_sz8lNhTZGqvQSRKDFlrNR2ZS0rTvDk6Q`，06:36:55 dispatcher 重启导致本地轮询/写回中断；上游实际已 `completed`。已按 task_id 恢复下载并上传附件，写回 `首帧图生成状态=成功`、file_token `E5FAb7hDvo8aNxxxRwcc8DGanVd`，未重新烧模型。

## 首尾帧生视频任务恢复机制

- 排查 `002-首尾帧视频生成表` 生视频长期 `待生成/生成中`：根因是 OTU `/v1/videos` 间歇性 524/SSL/timeout 触发 dispatcher 熔断；旧 dispatcher claim 会清空 `视频任务ID`，导致已有上游任务不能恢复、可能重复提交。
- 已修复 `tk_first_last_video.py`：`render_video()` 发现记录已有 `视频任务ID` 时优先恢复轮询该 task、下载 mp4、上传飞书并写回成功；只有无 task id 时才新提交 OTU。失败写回保留 task id 供下次恢复。
- 已修复 `tk_dispatcher.py`：`首尾帧视频生成` watch 不再 claim 时清空 `视频任务ID` / `视频生成原始响应JSON`；明确重生仍走 `视频操作=重新生成首尾帧视频`，该路径会主动清空旧 task 并递增版本。
- 线上孤儿记录 `recvkOMacJtAA9` 按已有 `task_FHs2UxdWn3RCkw8LRh7K0kDGAvBDiVRW` 执行恢复命令时返回成功，file_token `YXxvbIkHPoNswgxCW18cYYwGnIE`，未重新烧模型；但随后直接回查该旧记录返回 `RecordIdNotFound`，当前活跃表扫描也不再出现该旧批次记录。已清理 `tk_first_last_video.py::首尾帧视频生成` 熔断并重启 ryan dispatcher；后续仍可能受 OTU 网络抖动影响，但会保留 task id 继续恢复。
- 截图中的批量失败不是模型未生成，而是上游 OTU 已返回 mp4 URL 后，本机从 R2 `pub-*.r2.dev` 下载 mp4 时遇到 SSL EOF / read timeout。已给共享视频下载逻辑增加 4 次重试和较短读超时；现场用已有 task 恢复了场景 5/6，场景 7 也由 dispatcher 跑通，均未重复烧模型。
- 验证：`python3 -m unittest tk_toolkit_dual.test_first_last_video tk_toolkit_dual.test_shot_video` 通过 66 tests；`py_compile` 通过。

## 首尾帧首帧图片生成卡住排查

- 排查 `002-首尾帧视频生成表` 记录 `recvkTMGKbkGTs`（尿味2布艺-首尾帧-场景01-泰国公寓布艺沙发坐垫）：15:45:01 启动首帧图生成，16:03:12 OTU 首个图片任务 `task_lzrAPhKk3FYAgvgLsxrqcHFC5KVoK5it` 返回失败，错误为 `upstream_error / 没有可用token`，dispatcher 按可重试错误回退并于 16:03:58 启动第二个任务 `task_ljbahUayuMnGjD0WE1DZmVWV3Rj5iXKv`；只读查询显示第二个任务当前仍为 `queued`、progress=0，不是本地代码死循环。
- 同步修复首帧图片恢复隐患：`tk_first_last_video.py::render_first_frame()` 现在在记录处于 `首帧图生成状态=生成中` 且已有 `首帧图任务ID` 时，会复用已有 OTU 图片任务继续轮询、下载、上传和写回，不再重新提交新图片任务，也不强制重新解析/下载产品参考图；新增单测覆盖 `submit_otu_image_task` 不被调用的恢复路径。
- 验证：`python3 -m unittest tk_toolkit_dual.test_first_last_video tk_toolkit_dual.test_otu_image` 通过 35 tests；`python3 -m py_compile tk_toolkit_dual/tk_first_last_video.py tk_toolkit_dual/test_first_last_video.py` 通过。

## 项目避坑复盘沉淀

- 已把 2026-05-28 开发中踩过的坑整理进 `docs/tk-pipeline/tk-pipeline-lessons-learned.md` 第 8-16 节。后续开发类似多维表格 + dispatcher + 外部模型长任务功能时，优先回看这些规则：提示词真源单一、拆分配置不要混用、记录类型强筛选、task_id 恢复锚点、区分上游/本地失败、媒体下载重试、多参考图权重、Markdown 直拆优先、收口验证要覆盖旧口径残留和真实运行入口。
