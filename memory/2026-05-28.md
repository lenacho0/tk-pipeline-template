# 2026-05-28

## 故事板图片视频生成表：提示词真源与重生成链路

- 围绕 `001-故事板图片视频生成表` 落地了故事板图片 + Omni 图生视频链路的多轮修正：母任务由用户填 `脚本内容`、选择产品记录和一个或多个模特记录，可选环境图；产品图/产品名/目标人群来自 `初始化-产品信息`，模特照片和角色描述来自 `初始化-模特形象配置`。
- 故事板拆分提示词改为读取 `初始化-模型与API配置` 的 `故事板图片提示词拆分`。模型输出 Markdown 代码块中的 `Storyboard XX Prompt:`，代码只解析段落并原样写入子记录 `故事板图片提示词`，不再用代码模板二次改写最终图片 prompt。
- Omni 视频提示词改为读取配置表 `故事板视频生成-Omni` 的 `提示词`，默认内容为用户给定的 Markdown prompt。最终提交给视频 API 的 prompt 只剥离 Markdown 代码块和 `Omni Video Prompt:` 标题，不追加产品、Time Range、故事板提示词、参考图顺序或额外视频方向。
- 用户明确要求：最终提示词不要额外做其他功能，必须直接使用用户给出的系统提示词；任何增删改都要事先确认。
- 回归验证：`python3 -m unittest tk_toolkit_dual.test_storyboard_video` 通过 29 tests；`python3 -m unittest tk_toolkit_dual.test_storyboard_video tk_toolkit_dual.test_shot_video tk_toolkit_dual.test_script_doc_shots tk_toolkit_dual.test_shot_storyboard` 通过 86 tests。

## 首尾帧 Markdown 直拆与配置拆分

- `002-首尾帧视频生成表` 已改为严格 Markdown 直拆：只接受 `## S01 场景标题` + `### S01-1 首帧生图提示词` / `### S01-2 尾帧生图 / 编辑提示词` / `### S01-3 首尾帧图生视频提示词`。系统只提取代码块或标题下正文原文，不调用 Gemini，不做 JSON 兜底，不做自然语言改写；格式不对直接失败并提示修正。
- 飞书 `初始化-模型与API配置` 已拆清：旧 `文本拆分生成-Gemini` 改为 `脚本文档结构化拆分-Gemini`，只服务 003 三张脚本文档表；`故事板图片提示词拆分-Gemini` 补齐独立 Gemini 模型/API/提示词配置，只服务 001；002 只挂 `图片生成-OTU` 和 `分镜视频生成-OTU`。
- 本地配置 key 改为 `script_doc_text_split=recveQZPORmm33`、`storyboard_text_split=recvkP9VfsEY3o`、`main_image_otu=recveQZRZc7NUI`。后续不要再使用 `main_text_split` 或 `文本拆分生成-Gemini` 作为正式运行口径。
- 验证：`py_compile` 通过；相关单测 `130 tests OK`；`rg "文本拆分生成-Gemini|main_text_split|复用文本拆分生成-Gemini" tk_toolkit_dual -S` 无命中；ryan dispatcher 已 `launchctl kickstart`，healthcheck 显示 `Markdown直拆✓`。
