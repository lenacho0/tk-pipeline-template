# 2026-04-02

## 每日巡检 / 九宫格生成视频 对齐

- 用户指出：每日巡检逻辑里仍缺少最新功能“九宫格生成视频”，且已经删除的原老视频生成环节仍残留在巡检中，需要同步修正。
- 已先修正 dispatcher / 监控环节命名：
  - 文件：`tk_toolkit_dual/tk_dispatcher.py`
  - 将原先巡检/监控展示中的 `003视频生成` 更名为 `九宫格生成视频`
  - 实际执行链路保持不变，仍由 `tk_video_from_storyboard.py` 负责，状态字段仍为 `视频生成状态`
  - commit: `bd97d4c Rename video inspection stage to 九宫格生成视频`
- 随后继续补齐真正的每日巡检逻辑：
  - 文件：`tk_toolkit_dual/tk_healthcheck.py`
  - 新增专项检查项：`九宫格生成视频配置`
  - 巡检现在会检查以下 3 个配置环节是否存在且字段完整：
    - `九宫格生成视频-grok`
    - `九宫格生成视频-sora`
    - `九宫格生成视频-seeddance2.0`
  - 检查字段包括：
    - `模型名称`
    - `API Key`
    - `API 代理地址`
    - `提示词`
- 新增旧环节残留检测逻辑：
  - 若配置表里仍存在“名称包含 `视频生成`，但不属于 `九宫格生成视频-*`”的旧环节，healthcheck 会直接报异常，提醒存在旧配置残留。
- 本次修改已完成语法校验并提交：
  - `46acfeb Add 九宫格生成视频 checks to healthcheck`

## colleague 实例与 ryan 对齐

- 用户要求把 ryan 实例里本轮已完成的更新，同步到 colleague 实例。
- 已确认：代码层本就共用 `tk_toolkit_dual/`，因此 dispatcher / healthcheck / 脚本生成 / 视频生成 的逻辑修改天然同时作用于 ryan 与 colleague。
- 本次真正需要补齐的是 colleague 的配置表与字段结构；已完成以下同步：
  - 在 colleague 配置表新增三条视频环节：
    - `九宫格生成视频-grok` → `recvfDb07M9VxI`
    - `九宫格生成视频-sora` → `recvfDb0Rf1BNg`
    - `九宫格生成视频-seeddance2.0` → `recvfDb1kuNban`
  - 三条环节已同步 ryan 当前对应配置：`模型名称`、`API Key`、`API 代理地址`、`提示词`、`调用方式`。
  - colleague 旧视频环节 `视频制作`（`recvfjHzuDO9K1`）已删除，不再保留。
  - `tk_toolkit_dual/tk_healthcheck.py` 已进一步修正为从 `九宫格生成视频-sora` 读取 Sora 连通性配置，不再依赖旧 `video` 单配置口径；commit: `58bc698 Point Sora healthcheck to 九宫格生成视频-sora`。
  - colleague 的 003 产品脚本分镜图生成表已补齐与 ryan 一致的视频相关字段，且字段类型按 ryan 对齐，不是全部文本：
    - `是否生成视频`（单选）
    - `视频模型`（单选）
    - `视频生成状态`（单选）
    - `视频提示词`（文本）
    - `视频任务ID`（文本）
    - `生成视频`（附件）
    - `视频错误信息`（文本）
- 已执行一轮全量对齐审计：
  - 对比表：`config`、`fetch_config`、`data`、`analysis`、`script_gen`、`shot_script_gen`、`shot_storyboard`、`product`、`model`
  - 结果：colleague 相对 ryan 在关键运行层面的字段结构无缺失、无多余、无类型不一致。
  - 配置表环节也已对齐：无缺失、无额外、无关键配置差异。
- 关键结论：
  - **在关键运行配置、关键表结构、关键环节配置层面，colleague 现已与 ryan 对齐。**
  - 未做逐条历史业务数据一致性比对；“一模一样”指运行层面与结构层面，而非历史记录内容逐条相同。

## 用户偏好 / 工作方式

- 用户明确要求：当巡检、调度、功能命名发生迁移时，要同步更新每日巡检与相关监控口径，避免旧流程名残留造成误导。
- 用户要求本次关于巡检与长期演进约束的对话写入 memory，并将关键点沉淀到长期 memory。
