# 2026-04-02

## 每日巡检 / 九宫格生成视频 对齐

- 用户指出：每日巡检逻辑里仍缺少最新功能“九宫格生成视频”，且已经删除的原老视频生成环节仍残留在巡检中，需要同步修正。
- 已先修正 dispatcher / 监控环节命名：
  - 文件：`tk_toolkit_dual/tk_dispatcher.py`
  - 将原先巡检/监控展示中的 `003视频生成` 更名为 `九宫格生成视频`
  - 实际执行链路保持不变，仍由 `tk_video_from_storyboard.py` 负责，状态字段仍为 `视频生成状态`
  - commit: `bd97d4c Rename video inspection stage to 九宫格生成视频`
- 随后继续补齐真正的每日巡检逻辑：
  - 文件：`tk_toolkit_dual/tk_healthcheck.py`
  - 新增专项检查项：`九宫格生成视频配置`
  - 巡检现在会检查以下 3 个配置环节是否存在且字段完整：
    - `九宫格生成视频-grok`
    - `九宫格生成视频-sora`
    - `九宫格生成视频-seeddance2.0`
  - 检查字段包括：
    - `模型名称`
    - `API Key`
    - `API 代理地址`
    - `提示词`
- 新增旧环节残留检测逻辑：
  - 若配置表里仍存在“名称包含 `视频生成`，但不属于 `九宫格生成视频-*`”的旧环节，healthcheck 会直接报异常，提醒存在旧配置残留。
- 本次修改已完成语法校验并提交：
  - `46acfeb Add 九宫格生成视频 checks to healthcheck`

## 用户偏好 / 工作方式

- 用户明确要求：当巡检、调度、功能命名发生迁移时，要同步更新每日巡检与相关监控口径，避免旧流程名残留造成误导。
- 用户要求本次关于巡检与长期演进约束的对话写入 memory，并将关键点沉淀到长期 memory。
